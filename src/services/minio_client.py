"""Stage 7b: MinIO client - upload ảnh + JSON, generate URLs.

TODO: Phase 4 - implement:
- MinIOService.upload_image(image_bytes, key) → URL
- MinIOService.upload_json(data, key) → URL
- presigned_url() generator (7 days TTL)
- ensure_bucket(): tạo bucket nếu chưa có
"""
# Placeholder - sẽ implement Phase 4
